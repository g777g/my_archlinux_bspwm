## install Archlinux: 
	wifi connect:
- iwctl station wlan0 connect _ORBITA_

## run script tu install:
- curl -fsSL christitus.com/linux | sh
	System Setup
	Arch linux
	Arch server setup
- reboot

## connect wifi in clear ARCHLINUX:
- nmcli device wifi connect _ORBITA_ password "Ваш_пароль"

## install yay:
## run script tu install:
	- curl -fsSL christitus.com/linux | sh

## install pakeges:
	sudo pacmna -S ly xorg xorg-xinit micro mousepad bspwm sxhkd thunar thunar-archive-plugin xarchiver 	alacritty 
	git

## add autostart:
	cp .xinitrc /home/w22
	chmod +x .xinitrc

## create bspwm folder and files:
	- mkdir -p .config/bspwm
	- mkdir -p .config/sxhkd
	- cp /home/w22/.config/sxhkd/sxhkdrc
	- cp /home/w22/.config/bspwm/bspwmrc
	- chmod +x /home/w22/.config/sxhkd/sxhkdrc
	- chmod +x /home/w22/.config/bspwm/bspwmrc

## alacritty:
	- mkdir -p .config/alacritty
	- cp ~/.config/alacritty/alacritty.toml
## polybar
	- sudo pacman -S polybar brightnessctl nm-connection-editor networkmanager pulseaudio  pipewire pipewire-pulse bluez bluez-utils blueman playerctl ttf-jetbrains-mono-nerd scrot zip unzip feh
	- mkdir -p ~/.config/polybar
	- cp ~/.config/polybar/config.ini

	## щоб робила підсвітка
	- micro /etc/udev/rules.d/90-brightness.rules
		ACTION=="add", SUBSYSTEM=="backlight", KERNEL=="intel_backlight", RUN+="/bin/chgrp video /sys/class/backlight/%k/brightness", RUN+="/bin/chmod g+w /sys/class/backlight/%k/brightness"
	- sudo usermod -aG video w22
	- sudo udevadm control --reload-rules && sudo udevadm trigger
	- micro /etc/default/grub
	Додайте до рядка GRUB_CMDLINE_LINUX_DEFAULT параметр: i915.enable_dpcd_backlight=1


## install picom
	- sudo pacman -S picom
	- mkdir -p ~/.config/picom/
	- cp ~/.config/picom/picom.conf
	in micro tipe: ctrl + e set colorschemes catppuccin-frappe

## add language:
	- sudo micro /etc/X11/xorg.conf.d/00-keyboard.conf
		Section "InputClass"
			Identifier "system-keyboard"
			MatchIsKeyboard "on"
			Option "XkbLayout" "us,ua"
			Option "XkbOptions" "grp:alt_shift_toggle"
		EndSection

## install auto-mount-usb:
	- sudo pacman -S udisks2 gvfs gvfs-mtp gvfs-gphoto2 gvfs-afc polkit polkit-gnome


## instal GUEST vbox:
	- sudo pacman -S virtualbox-guest-utils
	- sudo systemctl enable --now vboxservice.service

## add to boot ly:
	- sudo systemctl enable ly@tty2.service

## install theme micro
	- mkdir -p ~/.config/micro/colorschemes
	- cp catppuccin-frappe.micro ~/.config/micro/colorschemes
	- cp settings.json ~/.config/micro

## install soft:
	- yay -S google-chrome

## install themes thunar Nordic
	- mkdir -p ~/.themes
	- cp -rf Nordic-master ~/.themes
	- micro .config/gtk-3.0/settings.ini
		[Settings]
		gtk-theme-name=Nordic-master













